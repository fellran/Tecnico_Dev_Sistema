Algoritmo "Repetição"

Var
i, j : inteiro
espacos, concat, letra : caractere

Inicio
      para i de 10 ate 0 passo -1 faca
      espacos <- espacos + "t"
      escreval(espacos)
      para j de i ate 10 passo 1 faca
      letra <- letra + "#"
      escreval(letra)
      fimpara
      fimpara

Fimalgoritmo